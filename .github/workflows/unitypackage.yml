name: Create Unity Package

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.package_name.outputs.tag }}
    steps:
    - uses: actions/checkout@v2
    - uses: ypicard/get-branch-name-github-action@v1
      id: names
    - run: echo "::set-output name=tag::${{ steps.names.outputs.tag }}
      id: tag

    - run: find Assets/ -name \*.meta > metaList
    - run: mkdir build

    - uses: pCYSl5EDgo/create-unitypackage@master
      with:
        package-path: 'build/InariUdon-${{ steps.names.outputs.tag }}.unitypackage'
        include-files: metaList

    - uses: actions/upload-artifact@master
      with:
        path: build
        name: package

  release:
    runs-on: ubuntu-latest
    needs:
      - build
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/download-artifact@master
        with:
          path: 'build/InariUdon-${{ needs.build.outputs.tag }}.unitypackage'
          name: 'InariUdon-${{ needs.build.outputs.tag }}.unitypackage'
      - name: Archive Release
        uses: thedoctor0/zip-release@master
        with:
          path: ${{needs.prebuild.outputs.package_name}}
          filename: 'InariUdon-${{ needs.build.outputs.tag }}.zip'
      - name: Release
        uses: fnkr/github-action-ghr@v1
        env:
          GHR_PATH: 'InariUdon-${{ needs.build.outputs.tag }}.zip'
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
